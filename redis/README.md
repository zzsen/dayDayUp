## redis
Redis是一个开源的使用**C语言**编写、支持网络、可**基于内存**亦可持久化的日志型、**Key-Value**的NoSQL数据库。

但是底层存储不是使用C语言的字符串类型，而是自己开发了一种数据类型SDS进行存储，SDS即**Simple Dynamic String** ，是一种动态字符串。

### 特点
#### 读写速度快
redis官网测试读写能到10万左右每秒。
速度快的原因:
1. 数据存储在内存中, 访问内存的速度是远远大于访问磁盘
2. Redis的**命令执行采用单线程**架构, 避免了上下文的切换和多线程带来的竞争, 也就不存在加锁释放锁的操作, 减少了CPU的消耗
   > 注意：Redis 6.0 引入了**多线程 I/O**（用于网络读写），但命令执行仍然是单线程的。多线程 I/O 只是为了提升网络吞吐量，不影响命令执行的线程安全性。
3. 采用了非阻塞IO多路复用机制
4. 高效的数据结构, 如哈希表、跳跃表、压缩列表等, 针对不同场景做了优化

### 数据结构丰富
Redis不仅仅支持简单的key-value类型的数据, 同时还提供list, set, zset, hash等数据结构。

### 支持持久化
Redis提供了RDB和AOF两种持久化策略, 能最大限度地保证Redis服务器宕机重启后数据不会丢失。

### 支持高可用
可以使用主从复制, 并且提供哨兵机制, 保证服务器的高可用。

### 客户端语言多
因为Redis受到社区和各大公司的广泛认可, 所以客户端语言涵盖了所有的主流编程语言, 比如Java, C, C++, PHP, NodeJS等等。

## 目录

- [五种数据类型底层实现原理](datatype.md)
- [持久化（RDB、AOF、混合持久化）](persistence.md)
- [数据库和缓存双写一致性](cache-db-consistency.md)
- [缓存穿透、缓存击穿、缓存雪崩](cache-avalanche.md)
- [为什么 Redis 集群有 16384 个槽](cluster-slots.md)
